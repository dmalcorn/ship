server {
    listen 80;

    # The full app (React SPA + API) is served from the API service.
    # Redirect all traffic there permanently.
    location / {
        return 301 https://api-production-71a9.up.railway.app$request_uri;
    }
}
