server {
    listen 80;

    # The app is now served from the API service (combined Express + React SPA).
    # Redirect all traffic there so users are not left with a broken nginx proxy.
    location / {
        return 301 https://api-production-71a9.up.railway.app$request_uri;
    }
}
